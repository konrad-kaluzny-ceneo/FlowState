"use client";

import type { OnboardingScope } from "~/lib/onboarding/types";

export function GuestHeaderControls(_props: { scope: OnboardingScope }) {
	// Theme and language controls moved to the Ustawienia (Settings) view.
	// This component remains as a placeholder for any future guest-only header controls.
	return null;
}
