> Detail reference — load on demand. Index: [roadmap.md](../../roadmap.md). Full stream: [`flow-coherence-recommendations.md`](../flow-coherence-recommendations.md).

# B-08

### B-08: Graceful session end while timer running

- **Outcome:** user can end the session calmly while a work or break cycle is running — via confirm-then-end path without waiting for cycle expiry or opaque interrupt-only UX.
- **Change ID:** fix-graceful-session-end-while-running
- **Linear:** —
- **GitHub:** —
- **PRD refs:** FR-019, FR-040, FR-012; FR-042 if paired with S-24
- **Prerequisites:** F-07 (gate ordering); S-24 for pause-then-end full variant
- **Parallel with:** —
- **Blockers:** OQ3 for full pause variant only
- **Unknowns:** Minimal = confirm → interrupt → closure (skip break) vs pause-then-end — owner: `/10x-plan`. Block: no for minimal.
- **Risk:** `endSession()` already interrupts server-side but UI disables button — minimal fix is UX + copy, not new backend. **Confidence: 72% minimal / 80% with S-24.**
- **Source:** user-flow T-04; `pomodoro-dashboard.tsx` `disabled={state === "running"}` on end-session btn.
- **Status:** proposed — P2; minimal after F-07; full after S-24
