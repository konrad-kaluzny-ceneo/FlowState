> Detail reference — load on demand. Index: [roadmap.md](../../roadmap.md). Source: test-plan Phase 8.

# Q-08: PRD v3 wedge coherence

- **Outcome:** quality rollout proves PRD v3 wedge coherence across transition mutex, pause semantics, optimistic wedge, network recovery, stuck-gate dismiss, and S-39 operability oracles.
- **Change ID:** testing-prd-v3-wedge-coherence
- **Linear:** —
- **GitHub:** —
- **PRD refs:** US-01, US-04; test-plan risks #8–#12.
- **Prerequisites:** F-07, S-21, S-24, S-28, S-34, S-35, S-39.
- **Parallel with:** none for S1; select one quality rollout before new product scope.
- **Blockers:** no hard blockers. External issue IDs are intentionally not created to avoid duplicate Linear/GitHub tracking.

## Notes

This is a quality companion, not a reopen of shipped S-39 product scope. Keep shipped product rows done and use test-plan Phase 8 as the implementation contract for the next research/plan handoff.

### S9 backlog sync (pre-PR, 2026-06-26)

External Linear/GitHub issue creation **skipped** — confirmed, not deferred:

- **No duplicate exists.** Searched Linear (`wedge coherence`, `PRD v3 wedge coherence`, `Q-08`, `testing-prd-v3-wedge-coherence`) and GitHub (`wedge coherence`, `testing-prd-v3-wedge-coherence`). Only matches are the already-shipped product slices this companion exercises — F-07/FLO-71, S-34/FLO-73, S-35/FLO-74, S-39/FLO-88 (#167) — plus unrelated backlog. No issue tracks this change-id.
- **Skill does not expect one.** `/10x-linear-backlog` imports only `F-NN` / `S-NN` roadmap IDs; `Q-08` is a quality-companion ID outside that pattern.
- **Lessons L-01/L-02.** Creating a product issue for surfaces already tracked by the shipped slices above risks duplicate two-way Linear ↔ GitHub tracking and the canonical-issue corruption those lessons document.
- **Contract lives elsewhere.** The implementation contract is test-plan §3 Phase 8 / §6.10, not a product issue.

Roadmap at-a-glance keeps Linear/GitHub as `—` for Q-08 by design. After PR [#169](https://github.com/konrad-kaluzny-ceneo/FlowState/pull/169) merged, the status moved to `done` without creating duplicate external issues.

- **Status:** done
