# Plan brief — S-33 break atmosphere

Shell break wash via `data-break-atmosphere` on home main; subdued task list; yields for wedge gates + break suggestion card. Pure helper + hook sync + CSS token overrides + tests.
