# Plan brief: revisit-user-choices

MVP proves **revisit pattern** on out-of-tab notification prefs: honest 3-state readout on timer hub, enable after Not now without overlay replay, permission request only on user-initiated enable from settings.

**Follow-on rollout:** apply same pattern (storage readout + timer-hub edit surface, no overlay replay) to check-in status, wedge dismissals, onboarding coaches.
